# The Content-Security-Policy hashes in hn_polarization.html cover its inline blocks byte for byte;
# a CRLF checkout would change them and the browser would refuse to run the page's scripts.
hn_polarization.html text eol=lf
